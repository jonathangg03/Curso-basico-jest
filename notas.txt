Arrays

-Export solo funciona para funciones, por lo que para exportar un array, lo declaramos, y lo retornamos dentro de
    un array.

Comparadores:
    .toContain(): Compara sí el array tiene el elemento que está como parametro.
    .toHaveLength(): Compara sí el array tiene el mismo tamaño que el colocado aquí.